# 合肥理工学院 · 宿舍尺寸指南 — 交接文档

## 一、项目概况

| 项目 | 内容 |
|------|------|
| 仓库 | `https://github.com/themoon12321/hfit-guide.git` |
| 线上地址 | `https://hfit-guide.pages.dev` |
| 托管 | Cloudflare Pages（自动从 GitHub main 分支部署） |
| 图片存储 | 腾讯云 COS：`https://hfit-guide-1459922161.cos.ap-nanjing.myqcloud.com` |
| 当前分支 | `main`（只有一个分支，扩建建议另开分支） |

---

## 二、目录结构

```
/
├── index.html              ← 主页面（所有 CSS+JS 合一）
├── about.html              ← 个人介绍页
├── editor.html             ← 热点标记工具（仅本地用，已 gitignore）
├── hotspots.js             ← 所有热点数据（坐标 + 文案 + 图片）
├── robots.txt              ← 屏蔽搜索引擎爬图片
├── .gitignore
│
├── img/
│   ├── ann/                ← 标注版图片（16 张，已上传 COS）
│   ├── raw/                ← 原图（20 张，已上传 COS）
│   ├── tips/               ← 碎碎念图片（8 张，已上传 COS，gitignored）
│   └── avatar.jpg          ← 头像（已上传 COS，本地废弃）
│
├── 图片素材-原始未标注/     ← 原始素材（不上传）
├── 照片素材-已标注/         ← 标注素材（不上传）
├── 碎碎念图片/              ← 碎碎念原图（不上传）
└── 图片编号对照表.md        ← 图片 ID 映射
```

---

## 三、图片映射规则

### COS 目录结构
```
COS 根目录/
├── img/
│   ├── raw/D01.jpg ~ D10.jpg, B01~B02, S01~S02, C01~C04, P01~P02
│   ├── ann/D01.jpg ~ D10.jpg（有标注的 16 张）
│   └── tips/ ← 碎碎念图片（用户手动上传）
```

### ID 命名规则
| 前缀 | 含义 |
|------|------|
| D01~D10 | 书桌 (Desk)，其中 D04~D06、D08 只有 raw 无 ann |
| B01~B02 | 床 (Bed) |
| S01~S02 | 楼梯 (Stair) |
| C01~C04 | 衣柜 (Closet) |
| P01~P02 | 空间示意 (Space) |

### 碎碎念图片（8 张，用户手动上传到 COS）
| 本地文件名 | COS 文件名 | 所属热点 |
|-----------|-----------|---------|
| USB灯.jpg | usb-light.jpg | D01 关于灯光 |
| 直插灯管.jpg | tube-light.jpg | D01 关于灯光 |
| 27寸显示器示意图.jpg | monitor-27.jpg | D01 关于显示器 |
| 柜挡板示意图.jpg | desk-baffle.jpg | D07 关于挡板 |
| 杆子衣架.jpg | pole-hanger.jpg | B02 关于伸缩杆 |
| 蚊帐杆缩短后状态.jpg | pole-shortened.jpg | B02 关于伸缩杆 |
| 货架示意图1~3.jpg | shelf-1~3.jpg | P01 关于侧边空间 |

**注意**：碎碎念图片 `img/tips/` 已 gitignore，需要手动上传 COS。

---

## 四、热点数据（hotspots.js）

### 结构
```javascript
const HOTSPOTS = {
  "D01": [
    { x:27.44, y:58.73, title:"关于显示器", text:"...", images:["COS_URL/..."] },
    ...
  ],
  ...
};
```

- `x, y`：百分比坐标（相对图片元素，编辑器取的）
- `title`：短标题
- `text`：详情文案（支持 `\n` 换行）
- `images`：可选，碎碎念图片 COS 完整 URL

### 当前覆盖
| 图片 | 热点数 | 内容 |
|------|--------|------|
| D01 书桌正视图 | 2 | 关于显示器 + 关于灯光 |
| D07 右下小柜 | 1 | 关于挡板 |
| B02 床板层高 | 1 | 关于伸缩杆 |
| S01 楼梯 | 1 | 关于楼梯 |
| S02 楼梯柜 | 1 | 关于楼梯抽屉 |
| C02 衣柜上层 | 1 | 关于上层衣柜 |
| C04 衣柜顶部 | 1 | 关于方管 |
| P01 侧边空间 | 1 | 关于侧边空间 |

**未标注（8 张）**：D02、D03、D09、D10、B01、C01、C03、P02

---

## 五、热点编辑器使用

打开 `editor.html`（本地文件，不要上传）：
1. 下拉选择图片 → 自动加载
2. 点击图片任意位置 → 添加热点
3. 拖动标记点 → 移动位置
4. 右侧列表 → 编辑标题/文案/删除
5. 点击"导出全部图片 JSON" → 复制到 `hotspots.js`

**坑**：坐标是相对于 `<img>` 元素的百分比。主页面已通过 `hotspot-layer` 精确匹配图片内容区域，解决手机端偏移问题。

---

## 六、关键技术踩坑记录

### 6.1 热点偏移（已修复）
**问题**：手机端热点位置漂移
**原因**：编辑器坐标相对 `<img>`（含 padding），但主页面 dots 相对 `.image-wrapper`（不含 padding 且 object-fit:contain 产生 letterbox）
**方案**：
- 新增 `hotspot-layer` 覆盖层（CSS `position:absolute`）
- `calcImageContentRect()` 计算图片实际渲染区域（去 padding + object-fit 计算）
- `updateHotspotLayer()` 动态调整覆盖层位置
- `toggleBubble()` 坐标基于 layer 而非 wrapper

### 6.2 气泡与 UI 重叠（已修复）
**原因**：气泡定位只检查视口边缘（10px），没考虑顶栏底栏
**方案**：
- `getSafeArea()` 动态获取顶栏/底栏高度
- 气泡用 safe area 约束位置
- 窄屏（<640px）气泡全宽适配

### 6.3 触摸误触翻页（已修复）
**原因**：全局 touch 事件没过滤热点区域
**方案**：`touchOnHotspot` 标志跳过热点/气泡区域的滑动检测

### 6.4 热点先于图片出现（已修复）
**原因**：`renderHotspots()` 在图片加载前就渲染
**方案**：
- 缓存命中时：换图同时渲染
- 未缓存时：等 `elImg.load` 事件触发后再渲染

### 6.5 内联 onclick 转义问题（已修复）
**问题**：`\'` 在 JS 字符串中被当成转义，传参是文字而非变量
**方案**：改用 `data-idx`/`data-total` 属性 + 事件委托监听

### 6.6 灯箱图片不显示（已修复）
**原因**：同上，引号转义导致传参错误
**方案**：`openLightbox(target.src, ...)` 事件委托

### 6.7 图片加载速度慢（已优化）
**原因**：标注图没预加载，冷启动等十几秒
**方案**：当前图加载完后后台逐张预加载其余图的 raw+ann 版本

### 6.8 GIT 相关
**问题**：中文文件名在 Cloudflare Pages 有问题
**方案**：全部改用英文 ID 命名（D01.jpg 而非 书桌01.jpg）
**问题**：混合换行符（CRLF/LF）导致 Edit 工具匹配失败
**应对**：改用 Python 脚本做文件替换

### 6.9 COS 跨域
已在 COS 控制台配置跨域（CORS）和 Referer 白名单，允许 Cloudflare Pages 域名访问。

---

## 七、关键代码位置（index.html）

| 功能 | 行号范围 | 说明 |
|------|---------|------|
| 图片数据 | ~396-414 | IMAGES 数组 |
| 热点渲染 | ~438-475 | calcImageContentRect → updateHotspotLayer → renderHotspots |
| 气泡弹窗 | ~478-520 | toggleBubble + 图片缩略图 + 事件委托 |
| 灯箱功能 | ~830-880 | openLightbox / closeLightbox / navigateLightbox |
| 触摸滑动 | ~730-750 | touchstart/touchend + touchOnHotspot 过滤 |
| 缩放 | ~570-640 | zoomEnabled + wheel/pointer/dblclick |
| 键盘 | ~676-695 | 方向键翻页 / Escape 首页 / A 切换标注 |
| 监听器 | ~896-915 | DOMContentLoaded 后绑定事件 |
| 图片加载 | ~500-540 | updateImage + 缓存逻辑 + 预加载 |
| resize | ~885-893 | 窗口变化重新定位热点层 |

---

## 八、未来扩展建议

### 模块化目录结构（待实施）
```
/
├── index.html              ← 主入口（各模块卡片导航）
├── dorm/                   ← 宿舍指南模块
│   ├── index.html          ← 当前 index.html 搬过来
│   ├── hotspots.js         ← 热点数据
│   └── editor.html         ← 热点编辑器
├── study/                  ← 学习指南模块（预留）
├── campus/                 ← 校园指南模块（预留）
└── about.html              ← 个人介绍（全局通用）
```

### 迁移注意事项
1. 图片路径需改为相对路径（如 `../img/raw/D01.jpg`）
2. `hotspots.js` 中的 COS URL 不变
3. Cloudflare Pages 部署根目录改为 `./`（不变）
4. `main` 分支保持不动，新开分支开发

---

## 九、部署

- 自动部署：push 到 GitHub main 分支 → Cloudflare Pages 自动构建
- 域名：`https://hfit-guide.pages.dev`
- 如果你有自己的域名，可以在 Cloudflare Pages 自定义域名里绑定
- 回退：`git revert` 或 `git reset` 到任意历史 commit

---

## 十、当前 to-do

- [ ] 核对所有图片尺寸数据
- [ ] 补充剩余 8 张图的热点（D02、D03、D09、D10、B01、C01、C03、P02）
- [ ] 上传 27寸显示器示意图 monitor-27.jpg 到 COS
- [ ] 考虑发小红书宣传
- [ ] 扩建为合肥理工学院指南（新分支开发）
